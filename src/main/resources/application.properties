spring.datasource.url=jdbc:oracle:thin:@${DATABASE_HOST:localhost}:${DATABASE_PORT:1521}/${DATABASE_NAME:XEPDB1}
spring.datasource.username=${DATABASE_USER:users_service}
spring.datasource.password=${DATABASE_PASSWORD:ciFw0RF6EL}
server.port=${PORT:9080}
jwt.secret-key=${JWT_SECRET_KEY:dummy-jwt-secret-key}
